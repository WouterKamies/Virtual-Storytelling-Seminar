using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OntriggerSnap : MonoBehaviour
{
    //Check ontriggerenter collider tag, set the main room to the corresponding snappoint

    //fake pressure plate in front of doors to open, works with an onTriggerEnter collider above it(each plate will have a tag, to mazeToMain, mainToMaze etc.)
    //Snap the corresponding rooms to the snappoints


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
